// UI-side exports — re-exports from build-config and parse-stdout, used by
// Paperclip's UI bundler when it loads the adapter package.

export { parseHermesStdoutLine } from "./parse-stdout.js";
export { buildHermesConfig } from "./build-config.js";
